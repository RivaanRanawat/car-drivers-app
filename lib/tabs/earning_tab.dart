import "package:flutter/material.dart";

class EarningTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Hello From Earning")
    );
  }
}